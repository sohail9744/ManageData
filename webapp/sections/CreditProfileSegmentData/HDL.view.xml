<mvc:View controllerName="Iffco.clap.sections.CreditProfileSegmentData.HDLController" xmlns:mvc="sap.ui.core.mvc" xmlns:forms="sap.ui.layout.form" xmlns="sap.m" xmlns:core="sap.ui.core">
	<forms:SimpleForm editable="false" layout="ColumnLayout">
    <core:Title text="Highest Dunning Level"/>
    <Label text="Data Outdated" />
		<CheckBox enabled="{appView>/status}" selected="{=${Customers>/zdata_outdated} === 'Y' ? true : false}" />

		<Label text="Dunning Level" />
		<Input enabled="{appView>/status}" value="{Customers>/zdunning_level}" maxLength="8" />
      
        <Label text="Amount" />
        <Input enabled="{appView>/status}" value= "{Customers>/zdunning_amt}" liveChange="handleSetMaxLength" maxLength="7" type="Number"/>

        <Label text="Document" />
        <Input enabled="{appView>/status}" value= "{Customers>/zdunning_doc}" maxLength="32"/>

        <Label text="No.Dunning Nots" />
        <Input enabled="{appView>/status}" value= "{Customers>/zdunning_not}" liveChange="handleSetMaxLength" maxLength="4" type="Number"/>
        


    </forms:SimpleForm>
</mvc:View>