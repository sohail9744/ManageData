<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns:forms="sap.ui.layout.form" xmlns="sap.m" >
	<forms:SimpleForm editable="false" layout="ColumnLayout">
		<Label text="Reason Code Conversion" visible="{=${fieldMappingModel>/368/visibility} === 'Y' ? true : false}" required="{=${fieldMappingModel>/368/mandatory} === 'Y' ? true : false}"/>
		<Input value="{Customers>/zreason_code_conversion}" visible="{=${fieldMappingModel>/368/visibility} === 'Y' ? true : false}"/>
		<Label text = "Selection Rule" visible="{=${fieldMappingModel>/396/visibility} === 'Y' ? true : false}" required="{=${fieldMappingModel>/396/mandatory} === 'Y' ? true : false}"/>
		<Input value="{Customers>/zselection_rule}" visible="{=${fieldMappingModel>/396/visibility} === 'Y' ? true : false}"/>
	</forms:SimpleForm>
</mvc:View>